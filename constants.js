// Here we will define all variables for database

const DB_USER = 'admin';
const DB_PASSWORD = 'Fulton1274';
const DB_HOST = 'firstproject.ooujivs.mongodb.net';
const API_VERSION = 'v1';
const IP_SERVER = 'localhost';

const JWT_SECRET_KEY = 'secret';

//export the information

module.exports = {
  DB_USER,
  DB_PASSWORD,
  DB_HOST,
  API_VERSION,
  IP_SERVER,
  JWT_SECRET_KEY,
};
